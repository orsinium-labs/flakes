<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="Hi, I'm Gram">
    <meta name="keywords" content="flake8, flake8 plugins, gram">
    <meta name="author" content="Gram (@orsinium)">
    <title>Flakes: list of flake8 plugins and their codes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  </head>

  <body>
    <header class="text-center">
      <h1>Flakes: list of flake8 plugins and their codes</h1>
      <p class="lead">
        <a href="https://github.com/orsinium-labs/flakes">source code</a>
      </p>
    </header>
    <main class="container">
      {% for plugin in plugins %}
        <h2>{{ plugin.name }}</h2>
        <ul>
          <li>
            <b>Version:</b> {{ plugin.version }}
          </li>
          {% if plugin.url %}
              <li>
                <b>Home page:</b> <a href="{{ plugin.url }}">{{ plugin.short_url }}</a>
              </li>
          {% endif %}
          {% if plugin.author %}
              <li>
                <b>Author:</b> {{ plugin.author }}
              </li>
          {% endif %}
        </ul>
        <table class="table">
          {% for code, message in plugin.codes %}
            <tr>
              <td>{{ code }}</td>
              <td>{{ message }}</td>
            </tr>
          {% endfor %}
        </table>
      {% endfor %}
    </main>

    <footer class="footer container-fluid text-white p-3 my-3">
      <p class="fs-1 text-center">
        <a href="https://github.com/sponsors/orsinium" target="_blank" class="text-white">
          support me
        </a>
      </p>
    </footer>
  </body>

</html>
